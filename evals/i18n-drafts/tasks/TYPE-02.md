# Size inline code correctly in article titles

When an article title contains inline `code`, the code is visibly too small relative to the rest of the `h1`.

Extend the existing heading-code treatment so code inside the article title has the same relative sizing and neutral background used in lower-level headings. Ordinary inline code in body text must keep its current appearance. Make the smallest shared-style change that solves the problem.
